# Final fix round 2 brief

Read this first — it is the complete fix scope from the scoped re-review.

## Required fixes

1. In `library/text/shiny_text/shiny_text.css`, correct the actual visual direction for `data-shiny-direction="left"`: with `background-size: 200% auto`, the left-to-right sweep is `background-position: 150% center` to `-50% center`. Keep `data-shiny-direction="right"` as the reverse `-50%` to `150%`. Update the comment accordingly.
2. Update the normal browser assertion so it checks these correct endpoints and labels the result accurately. Rerun the harness and capture complete output.
3. Remove the exact temporary untracked `.playwright-cli` snapshot files generated by this task; do not remove any source, subpage, library, plan, or SDD evidence files.
4. Update `task-4-report.md` and `progress.md` to record that the scoped re-review finding was fixed, with the true pre-implementation baseline limitation still stated plainly.

## Constraints

- Do not change `index.html`, `README.md`, `PROJECT.md`, `mainpage_component/profile_style.css`, task-planner subpages, external CELL_ANTENNA files, Graphify output, or the retained ShinyText JavaScript.
- Preserve reduced-motion and forced-colors solid-text behavior.
- Do not commit, push, or spawn subagents.

## Validation and report

- Run the CSS/source direction assertions and confirm `150% → -50%` for left-to-right and `-50% → 150%` for right-to-left.
- Run `node --check library/text/shiny_text/shiny_text.js`, the complete normal/reduced-motion browser harness, and `git diff --check`.
- Write the complete report to `.superpowers/sdd/2026-08-26-portfolio-story-effects/final-fix-round-2-report.md` and return status, changed paths, evidence, and concerns.
